{
  "name": "self-healing-test-framework-extension",
  "version": "2.0.0", 
  "description": "Chrome Extension for automated test element recording and execution with MCP server integration",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc --noEmit && npm run build:background && npm run build:content && npm run build:popup && npm run build:auth-capture",
    "build:background": "vite build --config vite.background.config.ts",
    "build:content": "vite build --config vite.content.config.ts",
    "build:popup": "vite build --config vite.popup.config.ts",
    "build:auth-capture": "vite build --config vite.auth-capture.config.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  }
}
