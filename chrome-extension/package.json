{
  "name": "mcp-only-test-runner-extension",
  "version": "2.0.0",
  "description": "MCP-only Chrome Extension requiring active MCP server connection - no fallback modes",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc --noEmit && npm run build:background && npm run build:content && npm run build:popup",
    "build:background": "vite build --config vite.config.ts",
    "build:content": "vite build --config vite.content.config.ts",
    "build:popup": "vite build --config vite.popup.config.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  }
}
